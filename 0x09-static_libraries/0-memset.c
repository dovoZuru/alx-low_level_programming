#include "main.h"

/**
 * *_memset - fills memory of b with constant bytes n
 * froma a memory pointed to by s
 * @s: the memory area to be filled
 * @b: the char used to fill the memory area
 * @n: the number of bytes to filled in the address of s
 *
 * Return: A pointer to the filled area s
 */
char *_memset(char *s, char b, unsigned int n)
{}
