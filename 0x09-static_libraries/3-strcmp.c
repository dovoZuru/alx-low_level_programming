#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: the first string
 * @s2: the second string
 * Return: the results of the comparison between the two strings
 */
int _strcmp(char *s1, char *s2)
{}
