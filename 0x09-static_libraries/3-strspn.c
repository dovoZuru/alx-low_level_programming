#include "main.h"

/**
 * _strspn - gets the length of a prefixx substring
 * @s: the initial segment
 * @accept: the accepted bytes of data
 * Return: the number of accpeted bytes
 */

unsigned int _strspn(char *s, char *accept)
{}
