#include "main.h"

/**
 * *_strchr -locates a character in a string
 * @s: the string
 * @c: the character
 * Return: the pinter to the first occurence of character c
 */
char *_strchr(char *s, char c)
{}
